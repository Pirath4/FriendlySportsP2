import 'package:flutter/material.dart';

class QuadrasView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("X10 Complexo Esportivo")),
      body: Center(child: Text("q1")),
    );
  }
}

class QuadraXavierView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Quadra de esportes Xavier")),
      body: Center(child: Text("q2")),
    );
  }
}
